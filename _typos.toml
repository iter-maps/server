[files]
# Local-only reference material and generated/verbatim files.
extend-exclude = ["concept/", ".build-map/", "LICENSES/", "Cargo.lock"]

[default.extend-words]
# Level-of-detail map tiers, not a misspelling of "load".
LOD = "LOD"
